import React, { Fragment } from 'react';
import PropTypes from 'prop-types';

const SynchronizePromptPanel = () => {
  return (
    <Fragment>
    
    </Fragment>
  );
};

SynchronizePromptPanel.propTypes = {

};

export default SynchronizePromptPanel;
